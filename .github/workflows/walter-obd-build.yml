name: Build Walter OBD APK

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'WalterOBD/**'
      - '.github/workflows/walter-obd-build.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Build debug APK
        working-directory: WalterOBD
        run: gradle assembleDebug --stacktrace
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: WalterOBD-v1-debug
          path: WalterOBD/app/build/outputs/apk/debug/app-debug.apk
